#!/bin/bash
# Copy the default app detection scripts into place.

. lib/apps.sh
. lib/stateHealth.sh
. lib/display.sh

for app in $(getApps); do
    echo "$app"
    listStateProblems "$app" | indent
done
