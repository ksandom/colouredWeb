#!/bin/bash
# Copy the default app detection scripts into place.

dirName="appState"

for app in apps/*; do
    echo "$app"
    for state in "$app/appState"/*; do
        if grep -q '# TODO Remove this line when this has been filled out.' "$state"; then
            echo "  $state is incomplete."
        fi
    done
done
